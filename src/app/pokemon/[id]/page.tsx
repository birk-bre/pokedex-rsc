"use client";

type Pokemon = {
  pokemonid: number;
  name: string;
  shiny: string;
  id: string;
};

export default function Pokemon(params: any) {
  return <div className="flex flex-col"></div>;
}
